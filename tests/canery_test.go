// +build unit

package tests

import (
	"github.com/stretchr/testify/assert"
	"testing"
)

func TestUnit_canery(t *testing.T) {
	assert.True(t, true, "The Canary test to see if unit test are working.")
}
